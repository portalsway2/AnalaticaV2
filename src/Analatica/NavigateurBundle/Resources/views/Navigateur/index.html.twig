{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Navigateur list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Navigateur</th>
                <th>Version</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('navigateur_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.navigateur }}</td>
                <td>{{ entity.version }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('navigateur_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('navigateur_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('navigateur_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
